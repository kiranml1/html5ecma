define(["jquery"],function($){
	return (function ( $, window, document, undefined ) {
		return function html5ecma(){
			this.init = function(){
				console.log("plugin is invoked");
			};
		};
	});
});
